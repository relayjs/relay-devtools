// @flow

const { connectToDevTools } = require('relay-devtools-core/backend');

// Connect immediately with default options.
// If you need more control, use `relay-devtools-core` directly instead of `relay-devtools`.
connectToDevTools();
