declare export function startServer(port: number): Promise<void>;
declare export function setContentDOMNode(node: HTMLElement): void;

export interface Subscription {
  close(): void;
};

declare export function connectToSocket(socket: Object) : Subscription;
