declare function startServer(port: number): Promise;
declare function setContentDOMNode(node: HTMLElement): void;

declare interface Subscription {
  close(): void;
};

declare function connectToSocket(socket: Object) : Subscription;
